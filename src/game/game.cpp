#include "game.h"
#include "draw.h"

void cmGame::run(void)
{
    init();
    
    while(1) {
        // Input functions
        
        if(1) {
            // Processing functions
            
            // Drawing functions
            mDraw->draw();
        }
    }
    
    return;
}
